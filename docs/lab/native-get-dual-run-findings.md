# Native GET dual-run findings

## Status

Pending first flag-on run on `feat/lab-native-get-dual-run`.

## Method

- `ll-legacy`: all `ENABLE_NATIVE_*_ROUTER=false` (restify GETs)
- `ll-modern`: all `ENABLE_NATIVE_*_ROUTER=true` (native GETs)
- Auth: lab golden client basic
- Pass: identical HTTP status per `/v2` list path

## Results

_Pending._
